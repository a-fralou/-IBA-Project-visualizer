import styled from 'styled-components';

export const Content = styled.main`
    grid-area: content;
    padding: 0;
    position: relative;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    width: 100%;
    max-width: 918px;
    margin: 0px auto;
`;

// Empty div for now
export const Logo = styled.div``;
